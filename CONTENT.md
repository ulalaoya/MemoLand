# CONTENT.md — הוספת תוכן ל-MemoLand

יש שתי דרכים להוסיף תוכן: דרך **דשבורד ההורים** (ללא קוד, לשימוש יומיומי) או דרך **קוד** (למפתחים, להרחבת בנק התוכן המובנה).

---

## 1. דרך דשבורד ההורים (מומלץ להורים ומורים)

1. במפה, **לחיצה ארוכה על הלוגו** (≈0.75 שנייה).
2. מזינים את קוד ההורים (ברירת מחדל `1234`).
3. עוברים ללשונית **"ניהול תוכן"**.

שם אפשר:

- **רשימת מילים לטירת האוצר** — כותרת (למשל "מילים באנגלית", "חומר למבחן") + פריט בכל שורה. כפתור "תצוגה מקדימה" מקריא את הרשימה כדי לוודא שההקראה נשמעת נכון.
- **משפט למערת ההדים** — משפט שההקראה תשמיע והילד ירכיב מאריחים.

התוכן נשמר מקומית (localStorage) ונכלל בסבבי החזרה של האפליקציה. ניתן לגבות אותו דרך **הגדרות → ייצוא נתונים**.

---

## 2. דרך קוד (למפתחים)

### בנק התוכן של מערת ההדים

הקובץ [`src/engines/echoesContent.ts`](src/engines/echoesContent.ts) מכיל:

- `SUBJECTS`, `VERBS`, `OBJECTS`, `EXTRAS` — אבני הבניין למשפטים אקראיים בעברית פשוטה (רמת כיתה ב'). הוספת מילים כאן מגדילה את מגוון המשפטים ב"הקשב וחזור".
- `TAP_ICONS` — האייקונים למשחק ההוראות. הוספת אייקון דורשת גם ציור SVG תואם ב-[`src/components/svg/TapIcon.tsx`](src/components/svg/TapIcon.tsx) (מזהה זהה).
- `STORIES` — סיפורים קצרים (~30 שניות) לשליפה מושהית. כל סיפור: `id`, `text`, ומערך `questions` (שאלה, תשובה נכונה, ומערך אפשרויות). התשובה חייבת להופיע בתוך האפשרויות.

**דוגמה להוספת סיפור:**

```ts
{
  id: 'story.zoo',
  text: 'ממו ביקר בגן החיות. הוא ראה שני אריות, פיל אחד וחמישה קופים משחקים על עץ.',
  questions: [
    { q: 'כמה אריות ממו ראה?', answer: 'שניים', options: ['אחד', 'שניים', 'שלושה'] },
    { q: 'איפה הקופים שיחקו?', answer: 'על עץ', options: ['על עץ', 'במים', 'על סלע'] },
  ],
}
```

### פרמטרי הרמות

כל כוונון הקושי מרוכז ב-[`src/config/curriculum.ts`](src/config/curriculum.ts) — אורך רצפים, זמני חשיפה, מספר שלבים, ספי מטבעות ודרגות, וסולם החזרות. שינוי כאן משפיע על כל האפליקציה בלי לגעת ב-UI.

### הוספת אתגר חדש (מנוע)

ראה את פרק ההרחבה ב-[`README.md`](README.md). בקצרה: מממשים `ExerciseEngine`, רושמים ב-`ENGINES`, ומחברים רכיב משחק ב-`GameHost`.

**כלל ברזל:** ה-`generate` חייב להיות דטרמיניסטי (אותו `seed` → אותו אתגר) והתשובה חייבת להיגזר מהגירוי. בדיקות ה-Vitest אוכפות זאת אוטומטית על כל מנוע ברשימה.
