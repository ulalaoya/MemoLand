# PLAN.md — תוכנית ומצב

## מה נמסר בשלב הזה

**שלד מלא + 2 ארצות עובדות end-to-end** (עמק המספרים, מערת ההדים), בהתאם להחלטת ההיקף.

### הושלם ✅

- **תשתית:** React + Vite + TypeScript, PWA, RTL מלא, טוקני עיצוב מדויקים מה-Style Guide.
- **מנועי תרגילים טהורים** (מנותקים מ-UI): 4 מנועי עמק המספרים + 2 מנועי מערת ההדים.
- **מנוע RNG דטרמיניסטי** (mulberry32) לבדיקות.
- **אלגוריתם המדרגה** (1–15, עלייה/ירידה, רצפת 70% מהשיא, יעד דיוק 75–85%).
- **מתזמן חזרות במרווחים** (10 דק' → סוף סשן → +1 → +3 → +7 ימים).
- **בונה הסשן היומי** (7 שלבים, רוטציה בין ארצות, תמיד עם ארץ חזקה אחת).
- **State + persistence** (localStorage, ייצוא/ייבוא/איפוס, עמידות ל-JSON פגום).
- **תגמולים:** מטבעות (+בונוס רצף/מהירות), 5 דרגות ממו עם ציוד גלוי, תיבת אוצר, מדליות, רצף ימים + מגן רצף.
- **קול:** SpeechSynthesis he-IL + מסך "הקש כדי להתחיל" (iOS), אפקטים ב-Web Audio (בלי קבצים).
- **גרפיקה:** SVG מקורי לגמרי — ממו (5 דרגות ציוד), 5 מדריכים, אייקוני משוב, מטבע/דגל/מדליה/תיבה, 6 רקעים אטמוספריים.
- **מסכים:** Start, Map (שביל מתפתל), Session (מנהל 7 השלבים), Treasure (זיקוקים + מטבעות מתגלגלים), Parent Dashboard.
- **דשבורד הורים:** גייט קוד, מדדים לפי ארץ, span/דקות/רצף/עקביות, גרף, המלצות דטרמיניסטיות, ניהול תוכן, הגדרות.
- **בדיקות Vitest:** 26 בדיקות עוברות (generators, מדרגה, חזרות, בונה סשן).

### נבדק בדפדפן ✅

מסך פתיחה → מפה → משחק רצף ספרות (עם משוב חיובי וחשיפת התשובה) → הקשב-וחזר → מעבר בין פעילויות → דשבורד הורים מלא. RTL תקין, אין גלישה אופקית במובייל (375px).

---

## מה נשאר (הרחבות עתידיות)

לפי סדר עדיפות מוצע:

1. **ארצות 3–6** — הוספת מנועים ורכיבי משחק:
   - יער התמונות: הבזק תמונה, זיכרון מרחבי בגרירה, "מה השתנה".
   - הרי התבניות: השלמת תבנית, רצפים מעורבים, מיון בכלל משתנה, חידות לוגיקה.
   - מסלול הזריזות: מצא הבדלים, N-back, התאמת סמלים (בסיס כבר קיים ב-`SpeedMatchGame`).
   - טירת האוצר: חשיפה מוגבלת בזמן, מאמן קיבוץ (chunking), פסקת ידע, תוכן הורה.
2. **מצב הורה להוראות פיזיות** (מערת ההדים) — מסך נעילה + אישור בסיום.
3. **תזכורת יומית** — שילוב Notifications API (התשתית בהגדרות מוכנה).
4. **QA מלא עם Fable 5** — 3 סימולציות סשן, מדידת זמן 18–22 דק', השוואת טוקנים מול Style Guide.

התשתית תוכננה כך שכל אלה הן *הוספות*, לא שינויים — המפה, הסשן, ההתאמה והדשבורד כבר יודעים לעבוד עם כל מנוע חדש.
