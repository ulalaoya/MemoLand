# DECISIONS.md — החלטות עיצוב וארכיטקטורה

תיעוד ההחלטות המהותיות והנימוק להן.

## סטאק — React + Vite + TypeScript
הפרומפט מגדיר `src/engines`, `config/curriculum.ts` ו-Vitest — כלומר TypeScript עם build tool. React נבחר כי מבנה הרכיבים בפרומפט (`components/games`, `components/dashboard`) תואם לו, וכי ניהול ה-state המרובה (סשן, התקדמות, דשבורד) נקי יותר מ-Vanilla. Vite נותן PWA סטטי קל לפריסה.

## היקף — שלד מלא + 2 ארצות
הספק המלא (6 ארצות × ~4 אתגרים + דשבורד + מתזמן + חנות) גדול מדי למסירה אחת איכותית. נבחר שלד מלא עם עמק המספרים ומערת ההדים עובדות לגמרי — כדי לתת בסיס אמין ומוכח להרחבה, ולא 24 אתגרים חצי-גמורים.

## גרפיקה — נכסי המקור מה-Branding Board
בעקבות פידבק שה-SVG שציירתי נראה שטוח לעומת האיור העשיר שב-Board, **הדמויות, הלוגו ותמונת ההירו נחתכים ישירות מה-Branding Board** (`public/characters/*.png`, `public/logo.png`, `public/logo-compact.png`, `public/hero.png`). הדמויות מוצגות ב"אריח" לבן מעוגל (בסגנון האייקון) דרך רכיב `Character`. החיתוך נעשה אוטומטית עם sharp + זיהוי גבולות (סריקת עמודות לבנות בין הדמויות). הרקעים האטמוספריים והאייקונים הפונקציונליים (מטבע, דגל, מדליה) נשארו SVG מקורי. **הערה:** דרגות ה-ציוד הגלוי של ממו (בנדנה/גלימה/כנפיים) שהיו ב-SVG כבר לא מוצגות על הדמות; הדרגה עדיין מתקשרת דרך תווית הדרגה ופס ההתקדמות במפה.

## גופנים — self-host (@fontsource)
הלוגו המקורי השתמש ב-Lilita One בתוך SVG, אבל דפדפנים מרנדרים `<text>` ב-SVG עם גופן חלופי לפני שהגופן נטען — ולכן הלוגו נראה בפונט לא נכון. תוקן בשתי דרכים: (1) הלוגו הוחלף בתמונה; (2) שלושת הגופנים (Rubik, Assistant, Lilita One) מוגשים מ-self-host דרך `@fontsource` (תת-קבוצות עברית+לטינית) במקום Google Fonts CDN — כך הם נטענים תמיד, מיידית, וגם **אופליין** (נשמרים ב-service worker של ה-PWA).

## מנועים מנותקים מ-UI
כל `ExerciseEngine` הוא פונקציה טהורה `(level, seed) => Challenge` עם `check` משלו. זה מאפשר בדיקות Vitest אמיתיות (אין פריט בלתי פתיר, התשובה נגזרת מהגירוי, דטרמיניזם) בלי לרנדר UI, ומפריד בין "מה התרגיל" ל"איך הוא נראה".

## ניסיון אחד לכל אתגר (במקום retry אינסופי)
כל אתגר = ניסיון אחד שנרשם למדרגה. בתשובה שגויה מוצג משוב חיובי ("כמעט!") + חשיפת התשובה הנכונה, ואז מתקדמים. זה שומר על רישום נקי (ניסיון אחד → סטטיסטיקה אחת), ומבטיח שהמסע תמיד מסתיים בהצלחה כי שלב 7 הוא קל-בוודאות. ההקלה השקטה אחרי 2 טעויות ממומשת כהורדת רמה אפקטיבית בתוך הסשן (`softenBy`).

## לבבות — מנגנון "דחיפה להצלחה" (לפי בקשת המשתמש)
הפרומפט סתר את עצמו לגבי לבבות (סעיף 4 "אין game over" מול סעיף 9 שמציג כרטיס לבבות). ההכרעה הסופית, לבקשת המשתמש: **3 לבבות לכל מסע, וכל טעות מורידה לב.** כשנגמרים הלבבות מופיע מסך עידוד — "נגמרו הלבבות! ממו לא מוותר אף פעם — רוצה לנסות שוב?" — ובאישור הלבבות מתמלאים חזרה ל-3 והמסע ממשיך. זהו מנגנון שמעודד להתמיד **בלי איבוד התקדמות ובלי game over אמיתי** (הדחיפה היא מוטיבציונית, לא עונשית). הלבבות מוצגים בכותרת הסשן; ההקלה השקטה (הורדת רמה אחרי 2 טעויות) פועלת במקביל כדי לעזור לילד להצליח. `--red` שמור ללבבות ולבנדנה של ממו; תשובה שגויה עצמה מסומנת בצבע הארץ, לא באדום.

## בלי זיהוי דיבור
לפי הפרומפט — לא אמין בעברית. תרגילי "הקשב וחזור" נבדקים בהרכבת אריחים על המסך; הוראות נבדקות בהקשה בסדר הנכון.

## מסך "הקש כדי להתחיל"
חובה ב-iOS: `SpeechSynthesis` ו-Web Audio חייבים להיפתח מאירוע מגע. מסך הפתיחה מאתחל את שניהם בלחיצה אחת, וגם משמש כמסך מותג נעים.

## fallback להקראה
במכשירים מסוימים `SpeechSynthesisUtterance.onend` לא נורה. לכן לתרגילים שמיעתיים יש טיימר-גיבוי שמעביר לשלב הקלט גם אם ההקראה לא דיווחה סיום — כדי שהמשחק לעולם לא "ייתקע".

## remount בין פעילויות (`key={idx}`)
רכיבי המשחק מחזיקים state פנימי (phase, קלט). מעבר בין פעילויות מאלץ remount דרך `key`, כדי שכל אתגר יתחיל נקי במקום לרשת את מצב ה"סיום" של קודמו. (באג שהתגלה ותוקן בבדיקות הדפדפן.)

## המלצות דטרמיניסטיות (לא LLM)
דשבורד ההורים משתמש בטבלת חוקים מפורשת ב-[`scheduler/recommendations.ts`](src/scheduler/recommendations.ts) — צפוי, ניתן לבדיקה, ובלי תלות ברשת. נאמנות לדרישת הפרומפט.

## persistence — localStorage עם debounce
פשוט, עובד אופליין, שורד רענון וסגירת דפדפן. שמירה עם debounce קצר כדי לא להעמיס בכתיבות. עמיד בפני JSON פגום (חוזר לברירת מחדל) וממזג שדות חדשים על טעינה (הגירת גרסאות).
